{
  "name": "gamebuds",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sync": "pnpm build && cap sync",
    "ios": "pnpm sync && cap run ios",
    "android": "pnpm sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "8.4.1",
    "@capacitor/core": "8.4.1",
    "@capacitor/ios": "8.4.1",
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.1",
    "typescript": "6.0.3",
    "vite": "8.1.4"
  }
}
