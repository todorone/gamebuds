# Browser development defaults to the local Worker.
VITE_API_URL=http://localhost:8787

# Installed and deployed clients must use an HTTPS Worker URL, for example:
# VITE_API_URL=https://api.example.workers.dev
