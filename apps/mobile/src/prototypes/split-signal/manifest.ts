import type { GameManifest } from '../../catalog/types';

export const splitSignalManifest: GameManifest = {
	id: 'split-signal',
	name: 'Split Signal',
	tagline:
		'Everyone sees one fragment. Repair the system by sharing what you know.',
	minPlayers: 2,
	maxPlayers: 4,
	status: 'prototype',
};
