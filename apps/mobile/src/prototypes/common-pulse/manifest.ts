import type { GameManifest } from '../../catalog/types';

export const commonPulseManifest: GameManifest = {
	id: 'common-pulse',
	name: 'Common Pulse',
	tagline:
		'One shared Pulse, one channel each. Keep it inside the moving band together.',
	minPlayers: 2,
	maxPlayers: 4,
	status: 'prototype',
};
